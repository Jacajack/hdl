module X{}
impl X{
	clock wire a = (clock wire)0u1, b = (clock wire)0u1, c = (clock wire)0u1;
	comb(a,b) wire d = 0;
	comb(a,b,c) wire e = (comb(a,b,c) wire)(d & c); // FIXME HIRN should eval this as async
}
